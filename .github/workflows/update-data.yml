name: Update Festival Data

on:
  schedule:
    - cron: "0 5 1 * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Verify TMDB_API_KEY secret is set
        run: |
          if [ -z "${{ secrets.TMDB_API_KEY }}" ]; then
            echo "::error::TMDB_API_KEY secret is not set. TMDB enrichment must run in CI so metadata isn't silently skipped."
            exit 1
          fi

      - name: Collect from APIs
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: npm run data:collect

      - name: Build comprehensive data
        run: npm run data:build

      - name: Download posters
        run: npm run data:posters

      - name: Validate data
        run: npm run data:validate

      - name: Build coverage report
        run: npm run data:coverage

      - name: Refresh data
        run: npm run data:update

      - name: Create pull request
        uses: peter-evans/create-pull-request@v7
        with:
          branch: chore/monthly-data-refresh
          commit-message: "chore: monthly archive data refresh"
          title: "chore: monthly archive data refresh"
          body: |
            Automated monthly update of normalized festival and awards datasets.
            - Sorted and refreshed nominations
            - Ready for curator review
